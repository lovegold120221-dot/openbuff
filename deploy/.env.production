# Required: Freebuff auth token (get from ~/.config/manicode/credentials.json)
FREEBUFF_TOKEN=your-token-here

# Optional: API key for client authentication
# Clients must send: Authorization: Bearer <API_KEY>
API_KEY=

# Optional: Caddy domain for HTTPS (leave empty to skip HTTPS)
CADDY_DOMAIN=
